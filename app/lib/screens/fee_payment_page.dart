import 'package:flutter/material.dart';

class FeePaymentPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Fee Payment")),
      body: Center(child: Text("Fee Payment Portal")),
    );
  }
}
